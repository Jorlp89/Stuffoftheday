# Standalone settings only. Never copy variables from another application.
PORT=3000
PUBLIC_BASE_URL=https://your-separate-railway-domain.example
CACHE_DIR=/tmp/thing-of-the-day-cache
FETCH_TIMEOUT_MS=3500
